# svg-curve-demo

React + TypeScript + Vite，全屏 SVG 曲线演示（开发端口 `5684`）。

## 命令（pnpm）

```bash
pnpm install
pnpm dev
pnpm build
pnpm preview
```

建议使用 [Corepack](https://nodejs.org/api/corepack.html) 固定 pnpm 版本（与 `package.json` 的 `packageManager` 字段一致）。
